import 'package:flutter/material.dart';

class DripColors {
  // Brand Colors
  static const Color lime = Color(0xFFD7FE72); // El verde de la pantalla 1
  static const Color blue = Color(0xFF0056D2); // El azul de la última pantalla
  
  // Backgrounds
  static const Color darkBg = Color(0xFF0A0A0A);
  static const Color cardBg = Colors.white;
  
  // Inputs & Greys
  static const Color darkInput = Color(0xFF1A1A1A);
  static const Color greyText = Color(0xFF8E8E8E);
}